import React from "react";
import ReactDOM from "react-dom";

// entry point
document.addEventListener("DOMContentLoaded", ()=>{
    const root = document.getElementById('root');

    ReactDOM.render(<div>Hello Fbook!</div>, root);
})
